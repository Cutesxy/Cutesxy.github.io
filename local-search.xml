<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>论文精读 | BERT：打破单向束缚的深度双向 Transformer</title>
    <link href="/2026/03/09/BERT-Note/"/>
    <url>/2026/03/09/BERT-Note/</url>
    
    <content type="html"><![CDATA[<blockquote><p><strong>论文链接</strong>：<a href="https://arxiv.org/abs/1810.04805">BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding</a><br><strong>一句话总结</strong>：BERT 抛弃了 Transformer 的 Decoder，仅保留 Encoder，通过“完形填空 (MLM)”和“下一句预测 (NSP)”两大任务，在海量无标注数据上炼就了拥有全局双向视野的语言理解大师，彻底统一了 NLP 的下游微调范式。</p></blockquote><h2 id="1-痛点与破局：为什么需要-BERT？"><a href="#1-痛点与破局：为什么需要-BERT？" class="headerlink" title="1. 痛点与破局：为什么需要 BERT？"></a>1. 痛点与破局：为什么需要 BERT？</h2><p>在 BERT 诞生前（2018年），主流的预训练模型（如初代 GPT、ELMo）都存在严重的<strong>视野受限</strong>问题：</p><ul><li><strong>GPT (自回归)</strong>：严格从左到右单向生成，采用 <code>subsequent_mask</code>（下三角掩码），模型无法提前看到后面的词。</li><li><strong>ELMo</strong>：虽然号称双向，但只是把从左到右和从右到左的特征在最顶层做了浅层拼接（Shallow Concatenation）。</li></ul><p><strong>BERT 的破局点</strong>：直接拆下 Transformer 的 Encoder 层，去掉所有的因果掩码（Causal Mask），让序列中的每一个 Token 从第一层开始就能<strong>全图双向可见</strong>。</p><h2 id="2-极致统一的输入表示-Input-Representations"><a href="#2-极致统一的输入表示-Input-Representations" class="headerlink" title="2. 极致统一的输入表示 (Input Representations)"></a>2. 极致统一的输入表示 (Input Representations)</h2><p>为了让模型能无缝接管“单句话分类”和“双句话问答”等各种繁杂任务，BERT 设计了一套极其优雅的“集装箱打包标准”。</p><p>一段连续的文本进入 BERT 前，会进行<strong>三位一体的暴力相加</strong>：</p><ol><li><strong>Token Embeddings</strong>：WordPiece 切词（词表大小 30,000）。句首强行插入 <code>[CLS]</code>（班长，用于聚合全局句子信息），句间插入 <code>[SEP]</code>（分隔符）。</li><li><strong>Segment Embeddings</strong>：引入 <em>E<sub>A</sub></em> 和 <em>E<sub>B</sub></em> 两个学出来的阵营向量，让模型分清哪些词属于第一句话，哪些属于第二句话。</li><li><strong>Position Embeddings</strong>：给每个位置发一个号码牌，不同于原版 Transformer 的三角函数，BERT 的位置向量是自己学出来的。</li></ol><h2 id="3-两大核心预训练神技-Pre-training-Tasks"><a href="#3-两大核心预训练神技-Pre-training-Tasks" class="headerlink" title="3. 两大核心预训练神技 (Pre-training Tasks)"></a>3. 两大核心预训练神技 (Pre-training Tasks)</h2><p>既然视野全开了，如果让它直接预测下一个词，模型就会“作弊”直接偷看答案。为此，BERT 发明了两个自监督任务：</p><h3 id="Task-1-掩码语言模型-Masked-LM-MLM"><a href="#Task-1-掩码语言模型-Masked-LM-MLM" class="headerlink" title="Task 1: 掩码语言模型 (Masked LM, MLM)"></a>Task 1: 掩码语言模型 (Masked LM, MLM)</h3><p>也就是暴力的“完形填空”。随机盖住句子中 15% 的词，让模型根据上下文去猜。<br>为了缓解预训练满眼都是 <code>[MASK]</code>，而微调时却没有 <code>[MASK]</code> 的“脱节”问题，作者设计了精妙的 <strong>80-10-10 妥协法则</strong>：</p><ul><li>80% 的时间替换为 <code>[MASK]</code></li><li>10% 的时间替换为一个随机词（注入噪音，逼迫模型结合上下文纠错）</li><li>10% 的时间保持原词不变（告诉模型：有时你看到的词是对的）</li></ul><h3 id="Task-2-下一句预测-Next-Sentence-Prediction-NSP"><a href="#Task-2-下一句预测-Next-Sentence-Prediction-NSP" class="headerlink" title="Task 2: 下一句预测 (Next Sentence Prediction, NSP)"></a>Task 2: 下一句预测 (Next Sentence Prediction, NSP)</h3><p>为了让模型理解句子间的逻辑关系，构建了一个廉价的二分类判断题：</p><ul><li>50% 的情况，句子 B 确实紧跟在句子 A 后面。</li><li>50% 的情况，句子 B 是从语料库里随机抓取的。<br>模型通过提取 <code>[CLS]</code> 对应输出的最终向量 <em>C</em>，来判断这两句话是否连续。</li></ul><h2 id="4-降维打击的微调范式-Fine-Tuning"><a href="#4-降维打击的微调范式-Fine-Tuning" class="headerlink" title="4. 降维打击的微调范式 (Fine-Tuning)"></a>4. 降维打击的微调范式 (Fine-Tuning)</h2><p>BERT 真正引发行业海啸的，是它极其低廉和统一的微调成本。</p><ul><li><strong>早期深度融合</strong>：不再像以前那样把两句话分别编码再做交互，而是直接拼成 <code>[CLS] A [SEP] B [SEP]</code> 送进模型，在底层 Attention 就完成了跨句子的深度交互。</li><li><strong>按需取件</strong>：<ul><li><strong>句子级任务</strong>（如情感分析）：直接取顶层 <code>[CLS]</code> 向量外接一个线性层。</li><li><strong>Token 级任务</strong>（如 NER、SQuAD问答）：取每个词对应的顶层输出向量 <em>T<sub>i</sub></em> 进行预测。</li></ul></li></ul><h2 id="5-个人思考与启发"><a href="#5-个人思考与启发" class="headerlink" title="5. 个人思考与启发"></a>5. 个人思考与启发</h2><ol><li><strong>大道至简的工程美学</strong>：把复杂的结构对齐问题，全部化简为 Token 序列的拼接；把深奥的上下文理解，化简为廉价的完形填空。</li><li><strong>GPT 与 BERT 的宿命分野</strong>：BERT 去掉 Decoder 走向了极致的<strong>阅读理解</strong>，GPT 去掉 Encoder 走向了极致的<strong>自回归生成</strong>。在 2026 年回头看，虽然大模型时代被单向生成模型（LLM）统治，但 BERT 留下的全局注意力思想，依然在今天的 Embedding 向量检索（如 RAG）甚至扩散大模型（如 LLaDA 的 Masked Diffusion）中熠熠生辉。</li></ol>]]></content>
    
    
    <categories>
      
      <category>AI &amp; LLMs</category>
      
    </categories>
    
    
    <tags>
      
      <tag>NLP</tag>
      
      <tag>深度学习</tag>
      
      <tag>Transformer</tag>
      
      <tag>论文精读</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>The Annotated Transformer 源码剖析与复现笔记</title>
    <link href="/2026/03/03/Transformer-Code-Note/"/>
    <url>/2026/03/03/Transformer-Code-Note/</url>
    
    <content type="html"><![CDATA[<h2 id="💡-项目本质与复现动机"><a href="#💡-项目本质与复现动机" class="headerlink" title="💡 项目本质与复现动机"></a>💡 项目本质与复现动机</h2><p>“Paper is cheap, show me the code.”<br>《Attention Is All You Need》虽然是开山之作，但忽略了许多工程细节。哈佛 NLP 团队的这个项目将论文原句与 PyTorch 代码逐行对应，是理解 Transformer 底层张量流转（Tensor Flow）和 Mask 机制的最佳学习材料。此笔记记录了我的本地复现过程与关键代码的理解。</p><h2 id="🛠️-个人复现感悟与思考"><a href="#🛠️-个人复现感悟与思考" class="headerlink" title="🛠️ 个人复现感悟与思考"></a>🛠️ 个人复现感悟与思考</h2><h3 id="1-宏观架构：Model-Architecture"><a href="#1-宏观架构：Model-Architecture" class="headerlink" title="1. 宏观架构：Model Architecture"></a>1. 宏观架构：Model Architecture</h3><p>整体大框架：核心 Encoder-Decoder 骨架 + 独立的 Generator 任务头</p><ul><li><strong>Encoder-Decoder 骨架：</strong> 负责核心的张量流转与上下文语义特征提取，输出高维隐状态（Hidden States）。</li><li><strong>Generator 任务头：</strong> 本质是一个线性分类器 + Softmax。它负责将 Decoder 输出的抽象向量，映射为词表空间上的概率分布（Logits -&gt; Probabilities），从而为后续的贪婪解码或采样策略提供依据。这种骨架与头部解耦的设计，正是如今大模型 <code>lm_head</code> 的雏形。</li></ul><h4 id="1-1-Encoder-核心架构与工程细节"><a href="#1-1-Encoder-核心架构与工程细节" class="headerlink" title="1.1 Encoder 核心架构与工程细节"></a>1.1 Encoder 核心架构与工程细节</h4><p>Encoder 并非单一的网络层，而是由 N=6 个同构的 Layer 首尾相连堆叠而成的流水线。在复现这部分底层源码时，有几个极具工业价值的架构巧思：</p><p><strong>a. 避免权重共享的“克隆陷阱” (<code>clones</code> 模块)</strong><br>在构建多层同构网络时，绝不能使用简单的列表乘法（如 <code>[layer] * 6</code>），这会在底层内存中导致 6 层网络指向同一组权重参数指针，使得网络强行同质化。源码中通过 <code>copy.deepcopy</code> 在内存中开辟独立空间，确保每一层拥有独立可训练的大脑；同时必须使用 <code>nn.ModuleList</code> 进行外层包装，这是向底层的 Autograd 引擎显式“注册参数”的必要步骤，以保证反向传播时能正确追踪梯度。</p><p><strong>b. LayerNorm (层归一化) 的“反悔机制”</strong><br>深度网络极易因矩阵连乘导致数值失控（梯度消失/爆炸）。LayerNorm 的核心公式为：</p>$$y = \frac{x - \mu}{\sigma + \epsilon} \cdot \gamma + \beta$$<ul><li><strong>独立维度的打磨：</strong> 源码中 <code>x.mean(-1)</code> 表明归一化是针对每个 Token 的 512 维特征独立进行的，词与词之间互不干扰。</li><li><strong>保留表达上限：</strong> 绝对的正态分布会抹杀数据原有的高阶语义。源码将 $\gamma$ 和 $\beta$ 定义为 <code>nn.Parameter</code>（可训练参数），赋予了网络在训练中通过梯度下降“撤销”强制归一化的能力，完美兼顾了前向数值稳定性与模型表达能力的上限。</li></ul><p><strong>c. 架构演进与知行不一：Pre-Norm 的残差高速公路</strong><br>原论文的公式定义是 <strong>Post-Norm (后归一化)</strong>：$Output = LayerNorm(x + Sublayer(x))$。但在哈佛团队的这份源码中，实际采用的是 <strong>Pre-Norm (前归一化)</strong>：$Output = x + Sublayer(LayerNorm(x))$。</p><ul><li><strong>底层原因剖析：</strong> Post-Norm 将残差相加后的总和进行统一归一化，导致反向传播时，梯度必须像剥洋葱一样穿透极其复杂的 LayerNorm 导数，极易造成梯度堵塞和训练崩溃。</li><li><strong>Pre-Norm 的优雅：</strong> Pre-Norm 仅对进入子层的<strong>副本</strong>进行打磨，而保留了外侧极其纯粹的加法残差主干道（Identity Path）。在微积分中，加法的导数为 1，这意味着无论网络堆叠多深，梯度都能顺着 <code>+</code> 号毫无衰减地“秒传”回最底层。这也是如今各类开源大模型无一例外全部采用 Pre-Norm 的根本原因。</li></ul><h4 id="1-2-Decoder-核心架构：自回归生成引擎"><a href="#1-2-Decoder-核心架构：自回归生成引擎" class="headerlink" title="1.2 Decoder 核心架构：自回归生成引擎"></a>1.2 Decoder 核心架构：自回归生成引擎</h4><p>相比于 Encoder 的“全局阅读”，Decoder 是一个“单向输出”的生成引擎。解码器同样由 N=6 个同构网络层堆叠而成。为了在机器翻译等任务中实现端到端的推理，Decoder 在单层架构中强行插入了第三个子层，形成了极其精密的“三段式”流水线：</p><p><strong>a. 戴上面罩的自查：Masked Self-Attention</strong><br>解码器允许每个位置关注到自身及其前面的所有位置。但在模型训练时，标准答案（Target）是一次性喂给矩阵的。为了保住模型“根据上文预测下文”的自回归特性（Auto-regressive property），必须从物理层面彻底斩断向左侧（未来）的信息流。</p><ul><li><strong>上三角掩码矩阵 (<code>subsequent_mask</code>)：</strong> 源码利用 <code>torch.triu(..., diagonal=1)</code> 生成了一个极其巧妙的上三角布尔矩阵。结合输出词嵌入向后偏移一位的设计，精准确保对位置 $i$ 的预测，仅仅依赖于小于 $i$ 的已知输出位置。</li></ul><p><strong>b. 跨越部门的进货：Encoder-Decoder Attention (交叉注意力)</strong><br>在这个核心层中，Query ($Q$) 来自解码器自身（代表当前正在生成的词语境），而 Key ($K$) 和 Value ($V$) 则全部提取自 Encoder 最后一层输出的全局记忆库（Memory）。</p><ul><li><strong>物理意义：</strong> 使得解码器在每生成一个词时，都能带着自己当前的疑惑，跨界去查阅输入序列的所有位置，寻找最匹配的线索。</li></ul><hr><h3 id="2-微观基石：Attention-引擎深度解构"><a href="#2-微观基石：Attention-引擎深度解构" class="headerlink" title="2. 微观基石：Attention 引擎深度解构"></a>2. 微观基石：Attention 引擎深度解构</h3><p>注意力机制的本质，是将 Query 和一组 Key-Value 对映射为输出的过程。</p><h4 id="2-1-核心公式：Scaled-Dot-Product-Attention"><a href="#2-1-核心公式：Scaled-Dot-Product-Attention" class="headerlink" title="2.1 核心公式：Scaled Dot-Product Attention"></a>2.1 核心公式：Scaled Dot-Product Attention</h4><p>在工业界的真实落地中，点积注意力凭借高度优化的底层矩阵乘法库，展现出了碾压级的速度和空间效率。哈佛源码忠实还原了核心公式：</p>$$Attention(Q, K, V) = softmax(\frac{QK^T}{\sqrt{d_k}})V$$<p><strong>a. 统计学救赎：为什么必须除以 $\sqrt{d_k}$？</strong></p><ul><li><strong>方差膨胀与梯度死亡：</strong> 假设 $Q$ 和 $K$ 的元素是独立的标准正态分布变量，它们点积结果的方差会随着维度累加剧烈膨胀至 $d_k$。极端大的点积数值会将 Softmax 推入平缓的死亡区域，导致梯度消失。</li><li><strong>缩放因子 (<code>math.sqrt(d_k)</code>)：</strong> 源码中 <code>scores / math.sqrt(d_k)</code> 这一行，精准地将点积得分的方差重新拉回至 1，使得数值分布回归温和，彻底拯救了 Softmax 的梯度流。</li></ul><p><strong>b. 面罩的物理切断 (<code>masked_fill</code>)</strong><br>在 Softmax 计算概率之前，源码执行了 <code>scores.masked_fill(mask == 0, -1e9)</code>。这一步将掩码矩阵中所有非法连接的原始得分强制替换为极小值（接近负无穷）。经过指数运算后，概率权重被绝对清零，从数学底层杜绝了“偷看未来”。</p><h4 id="2-2-并行视角的拓展：Multi-Head-Attention"><a href="#2-2-并行视角的拓展：Multi-Head-Attention" class="headerlink" title="2.2 并行视角的拓展：Multi-Head Attention"></a>2.2 并行视角的拓展：Multi-Head Attention</h4><p>单头注意力只能学到一种平均化的上下文表示。多头机制允许模型在不同位置，联合关注来自多个不同表征子空间的信息。在代码层面，这展现了极高的“张量体操”艺术：</p><ul><li><strong>避免循环的切片魔法 (<code>view</code> + <code>transpose</code>)：</strong> 源码没有创建 8 个小的 Linear 层，而是使用 4 个完整的 $512 \times 512$ 线性层。在一次性映射出巨大的 $Q, K, V$ 后，利用 <code>.view</code> 将 512 维优雅地切成 8 份，随后利用 <code>.transpose</code> 进行维度交换，在内存中瞬间开辟出 8 个相互平行的独立计算宇宙。</li><li><strong>延迟的跨维度融合 (<code>Concat</code> + <code>Linear</code>)：</strong> 在平行宇宙各自完成特征提取后，源码再次通过 <code>.transpose</code> 和 <code>.contiguous().view</code> 将张量重新拍扁拼接回 512 维。最后，送入全连接输出矩阵 $W^O$ 完成跨维度化学反应。这种“先独立发散，后全连接收网”的设计，构成了大语言模型复杂理解能力的基石。</li></ul><h3 id="3-其他核心组件：FFN、Embedding与位置编码"><a href="#3-其他核心组件：FFN、Embedding与位置编码" class="headerlink" title="3. 其他核心组件：FFN、Embedding与位置编码"></a>3. 其他核心组件：FFN、Embedding与位置编码</h3><p>在 Attention 实现了全局上下文的交互之后，模型还需要针对局部特征进行非线性映射，并为无序的序列注入位置信息。</p><h4 id="3-1-前馈神经网络-Position-wise-FFN"><a href="#3-1-前馈神经网络-Position-wise-FFN" class="headerlink" title="3.1 前馈神经网络 (Position-wise FFN)"></a>3.1 前馈神经网络 (Position-wise FFN)</h4><p>Attention 层负责跨 Token 的信息交互，而 FFN 层则负责对每个位置的 Token 进行独立的非线性变换。两者交替进行。</p>$$FFN(x) = \max(0, xW_1 + b_1)W_2 + b_2$$<ul><li><strong>升维与降维：</strong> 源码中，第一层线性变换 $W_1$ 将 512 维的隐向量映射到 2048 维，经过 ReLU 激活函数引入非线性后，再由第二层 $W_2$ 映射回 512 维。这种结构有助于模型在更高维空间中提取和解耦复杂的语义特征。</li></ul><h4 id="3-2-词嵌入与权重共享-Embeddings-amp-Softmax"><a href="#3-2-词嵌入与权重共享-Embeddings-amp-Softmax" class="headerlink" title="3.2 词嵌入与权重共享 (Embeddings &amp; Softmax)"></a>3.2 词嵌入与权重共享 (Embeddings &amp; Softmax)</h4><p>模型通过 Embedding 层将离散的文本 Token 映射为连续的高维向量。</p><ul><li><strong>权重共享 (Weight Tying)：</strong> 原论文和源码中，Encoder 的输入 Embedding、Decoder 的输入 Embedding，以及 Decoder 最后的 Pre-softmax 线性映射层共享了同一组权重矩阵。这不仅大幅减少了模型参数量，还使得输入和输出能够对齐在同一个语义空间中。</li><li><strong>Scale 缩放 (<code>* math.sqrt(d_model)</code>)：</strong> 源码在获取词向量后，乘以了 $\sqrt{512}$。因为 Embedding 初始化的方差较小，而接下来要相加的位置编码（正余弦函数）值域在 $[-1, 1]$ 之间。放大词向量的量级，是为了防止自身的语义特征被位置信号掩盖。</li></ul><h4 id="3-3-位置编码-Positional-Encoding"><a href="#3-3-位置编码-Positional-Encoding" class="headerlink" title="3.3 位置编码 (Positional Encoding)"></a>3.3 位置编码 (Positional Encoding)</h4><p>由于 Self-Attention 自身不具备处理序列顺序的能力，必须显式地引入位置信息。论文使用了不同频率的正弦和余弦函数：</p>$$PE_{(pos, 2i)} = \sin(pos / 10000^{2i/d_{model}})$$$$PE_{(pos, 2i+1)} = \cos(pos / 10000^{2i/d_{model}})$$<ul><li><strong>对数空间计算：</strong> 在 PyTorch 实现中，直接计算大底数幂容易导致下溢或速度较慢。源码利用 $a^{-x} = e^{-x \cdot \ln(a)}$，在 Log 空间内（利用 <code>torch.exp</code> 和 <code>math.log</code>）计算分母，是常见的工程加速与防溢出技巧。</li><li><strong>相对位置特性：</strong> 对于固定的偏移量 k，PE<sub>pos+k</sub> 可以表示为 PE<sub>pos</sub> 的线性组合。这使得 Attention 机制在点积时更容易学习到词与词之间的相对距离关系。</li><li><strong>Buffer 注册：</strong> 位置编码是固定的数学特征，不需要计算梯度。源码通过 <code>self.register_buffer(&quot;pe&quot;, pe)</code> 将其注册为模型的常量状态，避免被优化器更新。</li></ul><hr><h3 id="4-模型组装与推理逻辑简述"><a href="#4-模型组装与推理逻辑简述" class="headerlink" title="4. 模型组装与推理逻辑简述"></a>4. 模型组装与推理逻辑简述</h3><p>在完成所有子模块后，<code>make_model</code> 和 <code>inference_test</code> 展示了模型的完整组装和前向推理过程。</p><ul><li><strong>深拷贝隔离 (<code>copy.deepcopy</code>)：</strong> 在堆叠 6 层 Encoder 和 Decoder 时，必须使用深拷贝来实例化每一层，否则 PyTorch 底层会让多层网络共享同一块内存参数，导致网络层同质化。</li><li><strong>参数初始化：</strong> 组装完成后，源码对维度大于 1 的参数统一使用了 Xavier Uniform 初始化（Glorot 初始化），以保证前向传播时输入输出方差一致，维持训练早期的数值稳定性。</li><li><strong>自回归推理逻辑：</strong> 在文本生成时，Encoder 只需要对源序列执行一次前向传播生成 <code>memory</code>。随后 Decoder 进入循环：每次接收历史 Token 序列和 <code>memory</code>，经过 <code>subsequent_mask</code> 掩码处理，预测下一个 Token，并将其拼接到历史序列中送入下一轮循环。</li></ul><h3 id="5-模型训练与优化机制"><a href="#5-模型训练与优化机制" class="headerlink" title="5. 模型训练与优化机制"></a>5. 模型训练与优化机制</h3><p>模型的训练不仅依赖前向传播，还需要合理的批处理策略、学习率调度以及正则化手段来保证收敛的稳定性和泛化能力。</p><h4 id="5-1-数据批处理与掩码合并-Batching-amp-Masking"><a href="#5-1-数据批处理与掩码合并-Batching-amp-Masking" class="headerlink" title="5.1 数据批处理与掩码合并 (Batching &amp; Masking)"></a>5.1 数据批处理与掩码合并 (Batching &amp; Masking)</h4><p>在处理变长序列时，合理的批处理能够大幅提升 GPU 算力利用率。</p><ul><li><strong>动态批处理 (Dynamic Batching)：</strong> 原论文在训练时没有采用固定的句子数量作为 Batch Size，而是将长度相近的句子打包，使每个 Batch 包含固定数量的 Token（约 25000 个）。这种策略极大地减少了 <code>&lt;pad&gt;</code> 填充符的数量，提升了矩阵运算的有效载荷。</li><li><strong>Teacher Forcing 偏移输入：</strong> 在构造 Decoder 的输入和标签时，通过切片实现错位预测。<code>tgt[:, :-1]</code> 去掉最后一个词作为输入，<code>tgt[:, 1:]</code> 去掉第一个词作为预测目标。</li><li><strong>掩码的逻辑交集：</strong> Decoder 在自回归生成时，必须同时满足“不能看填充符”和“不能看未来词”两个条件。源码中通过逻辑与运算 <code>tgt_mask &amp; subsequent_mask</code> 获取两种掩码的交集，确保模型只关注合法的历史真实 Token。</li></ul><h4 id="5-2-训练流水线与显存管理-Training-Loop"><a href="#5-2-训练流水线与显存管理-Training-Loop" class="headerlink" title="5.2 训练流水线与显存管理 (Training Loop)"></a>5.2 训练流水线与显存管理 (Training Loop)</h4><p><code>run_epoch</code> 封装了标准的深度学习训练循环，并引入了适用于大模型训练的工程优化：</p><ul><li><strong>梯度累加 (Gradient Accumulation)：</strong> 受限于显存大小，模型通常无法一次性处理极大的 Batch。通过设置 <code>accum_iter</code>，模型在多个 Step 内仅执行反向传播 <code>.backward()</code> 累加梯度，达到指定次数后再调用 <code>optimizer.step()</code> 更新权重并清空梯度。这在工程上等效模拟了大 Batch Size 的平稳训练效果。</li><li><strong>防止显存泄漏：</strong> 每次计算完 Loss 并在反向传播结束后，源码通过 <code>del loss</code> 和 <code>del loss_node</code> 显式释放变量。这强制解除了对庞大计算图的引用，避免了随着循环推进导致显存溢出 (OOM)。</li></ul><h4 id="5-3-动态学习率调度-Noam-Schedule"><a href="#5-3-动态学习率调度-Noam-Schedule" class="headerlink" title="5.3 动态学习率调度 (Noam Schedule)"></a>5.3 动态学习率调度 (Noam Schedule)</h4><p>为了防止模型在随机初始化的早期因步长过大而导致梯度爆炸，作者提出了一种包含预热（Warmup）和衰减的动态学习率公式：</p><script type="math/tex; mode=display">lrate = d_{model}^{-0.5} \cdot \min(step\_num^{-0.5}, step\_num \cdot warmup\_steps^{-1.5})</script><ul><li><strong>两阶段调整：</strong><ol><li><strong>线性预热：</strong> 在前 <code>warmup_steps</code> 步（如 4000 步）内，公式选中后半部分，学习率呈线性稳步上升，帮助模型在初始阶段平稳建立优化方向。</li><li><strong>反平方根衰减：</strong> 超过预热期后，公式选中前半部分，学习率按 $1/\sqrt{x}$ 的规律平滑下降，在接近最优解时进行更细致的微调。</li></ol></li><li><strong>维度惩罚：</strong> 公式前方的 $d_{model}^{-0.5}$ 表明，模型的隐层维度越高（参数空间越复杂敏感），其整体学习率的峰值上限就会被压制得越低。代码中通过 PyTorch 的 <code>LambdaLR</code> 调度器实现了该策略。</li></ul><h4 id="5-4-正则化与标签平滑-Label-Smoothing"><a href="#5-4-正则化与标签平滑-Label-Smoothing" class="headerlink" title="5.4 正则化与标签平滑 (Label Smoothing)"></a>5.4 正则化与标签平滑 (Label Smoothing)</h4><p>在标准的交叉熵损失中，使用 One-Hot 编码要求模型将目标类别的概率推向 1.0。这容易导致模型过度自信（Overconfident）并在训练集上过拟合。</p><ul><li><strong>平滑概率分布：</strong> Label Smoothing 机制不再追求 100% 的绝对确信。通过引入平滑参数 $\epsilon$（如 0.1），将正确目标词的置信度降低到 $1 - \epsilon$（0.9），并将剩余的 $\epsilon$（0.1）概率质量均匀分配给词表中所有其他错误的 Token。</li><li><strong>KL 散度约束：</strong> 源码使用 <code>nn.KLDivLoss</code>（KL 散度）代替普通交叉熵，强制模型预测的输出概率去拟合这个人工调配的平滑分布。</li><li><strong>工程效果：</strong> 这种机制迫使模型去学习类间更丰富的软标签特征，而不是盲目放大正确类别的 Logit 权重。它容忍了一定的不确定性，从而提高了模型在面对未知数据时的泛化能力和 BLEU 评分。</li></ul><hr><p><strong>附录</strong>：</p><ul><li>官方源码仓库：<a href="https://github.com/harvardnlp/annotated-transformer">harvardnlp/annotated-transformer</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>Tech-Practice</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Transformer</tag>
      
      <tag>PyTorch</tag>
      
      <tag>Source-Code</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>GPT-1: Improving Language Understanding by Generative Pre-Training</title>
    <link href="/2026/03/03/GPT1-Note/"/>
    <url>/2026/03/03/GPT1-Note/</url>
    
    <content type="html"><![CDATA[<h2 id="📝-核心点"><a href="#📝-核心点" class="headerlink" title="📝 核心点"></a>📝 核心点</h2><p>Decoder-Only架构，仅解码，不编码，并且提出了pre-training and fine-tuning，坚持使用自回归（Autoregressive）方式，即“预测下一个 token”，利用仅预测next token的方式预训练模型，通过增加linear layer来微调全参使输出适配各种task，并且fine-tuning过程中的损失函数也很值得考量，一方面需要适配具体任务，另一方面也还需要继续学习原本的预测next token的任务，使得模型不会出现灾难性遗忘。文中隐隐感觉出现prompt提示词工程的思想和后续SFT的思想。当时性能不如 BERT，但 OpenAI 坚信“生成”才是通往通用的路径。</p>]]></content>
    
    
    <categories>
      
      <category>Paper-Reading</category>
      
    </categories>
    
    
    <tags>
      
      <tag>GPT</tag>
      
      <tag>NLP</tag>
      
      <tag>Generative-Pretraining</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Attention Is All You Need (Transformer) 精读笔记</title>
    <link href="/2026/03/03/Transformer-Note/"/>
    <url>/2026/03/03/Transformer-Note/</url>
    
    <content type="html"><![CDATA[<h2 id="📝-个人思考"><a href="#📝-个人思考" class="headerlink" title="📝 个人思考"></a>📝 个人思考</h2><p>提出了 Transformer 架构，引入 Self-Attention（自注意力机制）和 Positional Encoding（位置编码）。它解决了长距离依赖问题，并极大地提高了训练的并行度。</p>]]></content>
    
    
    <categories>
      
      <category>Paper-Reading</category>
      
    </categories>
    
    
    <tags>
      
      <tag>NLP</tag>
      
      <tag>Transformer</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
